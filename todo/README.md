# Flutter To-Do App (Local Persistence)

A simple To-Do List application built with Flutter. It supports:
- Add new tasks with a title
- View tasks in a scrollable list
- Mark tasks as complete
- Delete tasks
- Persist tasks locally using `shared_preferences` (closing the app does not erase data)

## Group Members

| # | Full Name | Registration Number | Role |
|---:|---|---|---|
| 1 | **(Add member name)** | (Reg No) | (Role) |
| 2 | **(Add member name)** | (Reg No) | (Role) |
| 3 | **(Add member name)** | (Reg No) | (Role) |

> Replace the table entries with your real group member details.

## How to Run


### Prerequisites
- Flutter SDK installed
- Android Studio (or VS Code) + Android emulator / physical device

### Steps
```bash
# 1) Get dependencies
flutter pub get

# 2) Run the app
flutter run
```

### Notes about the project structure
This ZIP contains the Flutter `lib/` code, configuration files, and screenshots.
If you need to regenerate missing platform folders (Android/iOS) on your machine, run:
```bash
flutter create .
flutter pub get
flutter run
```

## Screenshots (Demonstration)

Screenshots are inside the `screenshots/` folder:

- Main screen with the task list: `screenshots/main_screen.png`
- Adding a task: `screenshots/adding_task.png`
- Completing a task: `screenshots/completing_task.png`
- Deleting a task: `screenshots/deleting_task.png`

## Repository URL
After you push to GitHub, paste your repository URL here (example):
`https://github.com/username/flutter-todo-app`
